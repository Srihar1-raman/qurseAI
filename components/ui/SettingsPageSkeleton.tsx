/**
 * Settings Page Skeleton Component
 * Loading skeleton matching SettingsPageClient structure
 */

import { PageSkeleton } from './PageSkeleton';

export function SettingsPageSkeleton() {
  return <PageSkeleton variant="settings" />;
}

